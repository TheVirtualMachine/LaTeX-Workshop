\documentclass[12pt]{article} % Use the article class.

\usepackage[margin=1in]{geometry} % Use 1 inch margins.
\usepackage{parskip} % Simplified formatting of paragraphs.
\usepackage{amsmath} % Package from AMS that makes formatting LaTeX math easier.
\usepackage{amssymb} % AMS package for mathematical symbols.
\usepackage{lipsum}
\title{Quadratic Proof} % The title of our document.
\author{Vincent Macri \and JJ Marr} % The author's name.
\date{March 29, 2017} % The date of this document.

\frenchspacing % LaTeX puts extra space after periods by default. This line disables that.

\begin{document}
	\pagenumbering{gobble}
	\maketitle % Make the title page.
	\pagenumbering{roman}
	\tableofcontents % Create the table of contents.
	\newpage % Page break
	\pagenumbering{arabic}
	\section{Deriving} % Create a new section.
	Let's prove by deriving!
	Fairly trivial, proof is left to the proofreader.
	\begin{align}
		ax^2 + bx + c &= 0\\ %quadratic equation
		x^2 + \frac{b}{a}x + \frac{c}{a} &= 0\\ %divide both sides by a, which is non-zero
		x^2 + \frac{b}{a}x &= - \frac{c}{a}\\ %move c/a to the other side
		x^2 + \frac{b}{a}x + (\frac{b}{2a})^2&= - \frac{c}{a} + (\frac{b}{2a})^2\\ %set up a completion of the square
		(x + \frac{b}{2a})^2 &= - \frac{c}{a} + \frac{b^2}{4a^2}\\ %complete actual square and simplify right side
		(x + \frac{b}{2a})^2 &= \frac{b^2 - 4ac}{4a^2}\\ %simplify right side further
		x + \frac{b}{2a}&= \pm\frac{\sqrt{b^2 - 4ac}}{2a}\\ %square root of both sides, note the \pm used to show that a root in this context can either be positive or negative.
		x&= \frac{- b \pm \sqrt{b^2 - 4ac}}{4a^2} %move b/2a to the other side.
	\end{align}
	\subsection{Lorem Ipsum}
	\lipsum[4-57]
\end{document}
